import { Card, CardHeader, CardTitle, CardContent } from "@/components/ui/card";

export default function SchoolsPage() {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Schools</CardTitle>
      </CardHeader>
      <CardContent>
        <p>Manage your schools here.</p>
      </CardContent>
    </Card>
  );
}
